---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

import GrhHero from '../components/hero/GrhHero.astro';
import GrhSection from '../components/sections/GrhSection.astro';
import GrhDualPortal from '../components/sections/GrhDualPortal.astro';
import GrhTraditionShowcase from '../components/sections/GrhTraditionShowcase.astro';
import GrhTourismGrid from '../components/sections/GrhTourismGrid.astro';
import GrhOrtsteilSpread from '../components/sections/GrhOrtsteilSpread.astro';
import GrhEventsList from '../components/sections/GrhEventsList.astro';

import GrhNewsCard from '../components/cards/GrhNewsCard.astro';

import GrhButton from '../components/primitives/GrhButton.astro';
import GrhIcon from '../components/primitives/GrhIcon.astro';

import { getCurrentSeason } from '../lib/seasons';
import { getNews } from '../lib/cms';

const newsAll = await getNews();
const featuredNews =
  newsAll.find((n) => n.featured && n.category !== 'sperrung') ??
  newsAll.find((n) => n.category !== 'sperrung') ??
  newsAll[0];
const otherNews = newsAll.filter((n) => n.slug !== featuredNews.slug && n.category !== 'sperrung').slice(0, 3);
const sperrungen = newsAll.filter((n) => n.category === 'sperrung').slice(0, 3);

const categoryLabel: Record<string, string> = {
  verwaltung:    'Verwaltung',
  veranstaltung: 'Veranstaltung',
  sperrung:      'Sperrung',
  tourismus:     'Tourismus',
};
const severityLabel: Record<string, string> = {
  info:  'Hinweis',
  warn:  'Einschränkung',
  alert: 'Vollsperrung',
};
const fmtUntil = new Intl.DateTimeFormat('de-DE', { day: '2-digit', month: 'long' });

const events = (await getCollection('events'))
  .sort((a, b) => a.data.startDate.valueOf() - b.data.startDate.valueOf())
  .slice(0, 8)
  .map((e) => ({
    title: e.data.title,
    startDate: e.data.startDate,
    endDate: e.data.endDate,
    location: e.data.location,
    ortsteil: e.data.ortsteil,
    teaser: e.data.teaser,
    featured: e.data.featured,
    image: e.data.image,
    href: `/veranstaltungen/${e.slug}`,
  }));

const ortsteile = (await getCollection('ortsteile')).sort((a, b) => a.data.order - b.data.order);
const ortsteilSpread = ortsteile.map((o, i) => ({
  index: i + 1,
  name: o.data.name,
  key: o.data.key,
  tagline: o.data.tagline,
  description: o.data.description,
  image: o.data.image,
  href: `/gemeinde/ortsteile/${o.data.key}`,
}));

const season = getCurrentSeason();
const showAdventSection = season === 'advent';

const featured = showAdventSection
  ? {
      eyebrow: 'Advent',
      title: 'Lebendiger Adventskalender',
      subtitle: 'Jeden Abend ein neues Türchen — ein Lichtermeer aus Schwibbögen, Engeln und Bergmann.',
      ctas: [{ label: 'Programm 2026', href: '/leben/lebendiger-adventskalender', variant: 'primary' as const }],
    }
  : {
      eyebrow: 'EURORANDO 2026',
      title: 'Zwei Touren auf dem Zschopautalweg',
      subtitle: '23. & 27. September · Anmeldung offen',
      logo: '/images/logos/eurorando-2026-white.webp',
      logoAlt: 'EURORANDO 2026',
      ctas: [
        { label: 'Anmelden', href: '/veranstaltungen/eurorando-2026', variant: 'primary' as const },
      ],
    };

const fmtDate = new Intl.DateTimeFormat('de-DE', { day: '2-digit', month: '2-digit', year: 'numeric' });
---
<BaseLayout
  header="compact"
  title="Gemeinde Grünhainichen · Borstendorf · Waldkirchen im Erzgebirge"
  description="Offizielle Website der Gemeinde Grünhainichen mit den Ortsteilen Borstendorf, Grünhainichen und Waldkirchen/Erzgeb. Heimat der Wendt-&-Kühn-Engel und des Schachwanderwegs."
>
  <GrhHero
    season={season}
    featured={featured}
    slides={[
      {
        src: '/images/hero/floeha-antje-wolfeil.jpeg',
        alt: 'Die Flöha im Gemeindegebiet Grünhainichen',
        caption: 'Flöha im Herbst',
        credit: 'Antje Wolfeil',
      },
      {
        src: '/images/ortsteile/borstendorf-schachwanderweg.jpg',
        alt: 'Schachfiguren am Schachwanderweg mit Blick auf Borstendorf',
        caption: 'Schachwanderweg Borstendorf',
        credit: 'TVE / Bernd März',
      },
      {
        src: '/images/tradition/wendt-kuehn-freilandspieldose.jpg',
        alt: 'Freilandspieldose von Wendt & Kühn im Frühling',
        caption: 'Wendt-&-Kühn-Welt',
      },
      {
        src: '/images/ortsteile/waldkirchen-blaufarbenwerk.jpg',
        alt: 'Historischer Platz im Blaufarbenwerk Zschopenthal, Waldkirchen',
        caption: 'Blaufarbenwerk Zschopenthal',
      },
      {
        src: '/images/tradition/fuchsturm.jpg',
        alt: 'Der Fuchsturm im Mai',
        caption: 'Fuchsturm',
      },
    ]}
  />

  {/* === Schnelleinstieg: zwei Karten direkt unter dem Hero =============== */}
  <GrhSection tone="cream" padding="md">
    <GrhDualPortal />
  </GrhSection>

  {/* === 1) Editorial-Manifest =========================================== */}
  <GrhSection tone="cream" padding="lg" pattern="topo" align="left">
    <div class="grh-manifest">
      <div class="grh-manifest__lead">
        <span class="grh-eyebrow">Willkommen</span>
        <h2 class="grh-manifest__head grh-text-reveal">
          Eine Gemeinde aus drei Charakteren.
          <em>Holzkunst, Schachfiguren und ein Wald,
            der nach Tannen und Geschichten riecht.</em>
        </h2>
      </div>
      <div class="grh-manifest__copy">
        <p>
          Grünhainichen liegt zwischen Zschopau und Flöha — kein Touristenposter,
          sondern ein Stück gewachsenes Erzgebirge. Drei Ortsteile, jeder mit eigenem
          Wappen, eigener Tonlage, eigenem Selbstverständnis. Was sie verbindet, ist
          ein leiser Stolz darauf, dass hier Dinge entstehen, die anderswo nur erzählt werden.
        </p>
        <p class="grh-manifest__sig">
          <strong>Borstendorf</strong> · <strong>Grünhainichen</strong> · <strong>Waldkirchen / Erzgeb.</strong>
        </p>
      </div>
    </div>
  </GrhSection>

  {/* === Stacking Cards: Sektionen 2, 3, 4 stapeln sich beim Scrollen ===== */}
  <div class="grh-cardstack">
    <div class="grh-cardstack__card grh-cardstack__card--1">
      <GrhSection tone="cream" padding="lg" eyebrow="01 — Drei Ortsteile" title="Eine Heimat, drei Charaktere." align="left">
        <GrhOrtsteilSpread items={ortsteilSpread} />
      </GrhSection>
    </div>

    <div class="grh-cardstack__card grh-cardstack__card--2">
      <GrhSection tone="paper" padding="lg" pattern="topo">
        <GrhTraditionShowcase
          topic="wendt-kuehn"
          numeral="11"
          numeralLabel="Punkte auf jedem Engelflügel"
          eyebrow="02 — Holzkunst seit 1915"
          title="Die Engel mit den elf Punkten."
          body="In der Wendt & Kühn KG entstehen seit 1915 die berühmten Grünhainichener Engel® — kugelrunder Kopf, weißes Hemd mit hellblauen Akzenten, grüne Flügel mit genau elf weißen Punkten. Die Wendt-&-Kühn-Welt zeigt Schauwerkstatt, Manufakturgarten und einen historischen Musterschrank mit über 600 Figuren. Eintritt frei."
          pullQuote="Aus Lindenholz werden Geschichten."
          image="/images/tradition/wendt-kuehn-freilandspieldose.jpg"
          imageAlt="Die Freilandspieldose von Wendt & Kühn im Frühling"
          facts={[
            { value: '1915',  label: 'gegründet' },
            { value: '11',    label: 'weiße Punkte' },
            { value: '> 600', label: 'Figuren im Musterschrank' },
          ]}
          ctas={[
            { label: 'Wendt-&-Kühn-Welt besuchen', href: '/tourismus/sehenswuerdigkeiten' },
            { label: 'Werkstatt entdecken',         href: '/tourismus/sehenswuerdigkeiten#werkstatt', variant: 'ghost' },
          ]}
        />
      </GrhSection>
    </div>

    <div class="grh-cardstack__card grh-cardstack__card--3">
      <GrhSection tone="cream" padding="lg">
        <GrhTraditionShowcase
          topic="schachdorf"
          numeral="1871"
          numeralLabel="Erste Schachbrettfabrik"
          eyebrow="03 — Schachdorf seit 1871"
          title="Das Dorf, in dem Schach Pflichtfach war."
          body="1871 entstand in Borstendorf nach einem Großauftrag der Leipziger Messe die erste Schachbrettfabrik. In Hochzeiten produzierte K.P. Uhlig 7.000 Schachsätze pro Monat — bis zum DDR-Ende war Borstendorf alleiniger DDR-Schachhersteller. 1937 stand Schach hier sogar als Pflichtfach im Stundenplan. Heute trägt der Schachwanderweg dieses Erbe weiter: 14 km, 16 überlebensgroße Figuren."
          pullQuote="Vom Springer bis zur Dame: ein Wanderweg, der Schach spielt."
          image="/images/ortsteile/borstendorf-schachwanderweg.jpg"
          imageAlt="Schachfiguren am Borstendorfer Schachwanderweg mit Blick auf den Ortsteil"
          facts={[
            { value: '1871',   label: 'erste Schachbrettfabrik' },
            { value: '7.000',  label: 'Schachsätze pro Monat (Uhlig)' },
            { value: '14 km',  label: 'Schachwanderweg' },
          ]}
          reverse={true}
          ctas={[
            { label: 'Schachwanderweg entdecken', href: '/tourismus/wandern' },
            { label: 'Schachtraditionszimmer',    href: '/tourismus/sehenswuerdigkeiten#schachzimmer', variant: 'ghost' },
          ]}
        />
      </GrhSection>
    </div>

    <div class="grh-cardstack__card grh-cardstack__card--4">
      <GrhSection tone="paper" padding="lg" pattern="topo">
        <GrhTraditionShowcase
          topic="blaufarbenwerk"
          numeral="1687"
          numeralLabel="Gründung Blaufarbenwerk Zschopenthal"
          eyebrow="04 — Waldkirchen / Erzgeb. seit 1687"
          title="Das Blau, das die Welt malte."
          body="Im Ortsteil Zschopenthal entstand 1687 das Blaufarbenwerk — eine der ältesten Manufakturen für Kobaltblau in Europa. Über 160 Jahre lang lieferte sie das satte Blau, mit dem in Meißen, Delft und China Porzellan gemalt wurde. Heute zeugen das Ensemble alter Werksgebäude, die Neurenaissance-Kirche von 1900/01 und der Bahnhof an der Zschopautalbahn von dieser Zeit."
          pullQuote="Aus Erzgebirgsstein wurde das Blau für Europas Porzellan."
          image="/images/ortsteile/waldkirchen-blaufarbenwerk.jpg"
          imageAlt="Historischer Platz im Blaufarbenwerk Zschopenthal mit Fachwerkhäusern und Kirchturm"
          facts={[
            { value: '1687',   label: 'Gründung der Manufaktur' },
            { value: '162',    label: 'Jahre Kobaltblau-Tradition' },
            { value: '1900',   label: 'Neurenaissance-Kirche errichtet' },
          ]}
          ctas={[
            { label: 'Blaufarbenwerk entdecken', href: '/gemeinde/ortsteile/waldkirchen' },
            { label: 'Zschopautalbahn',          href: '/tourismus/wandern', variant: 'ghost' },
          ]}
        />
      </GrhSection>
    </div>
  </div>

  {/* === 5) Tourismus als horizontaler Streifen ========================== */}
  <GrhSection tone="forest-deep" padding="lg" pattern="topo" eyebrow="Erleben & Entdecken" title="Sechs Wege, die Gemeinde zu entdecken." align="left">
    <GrhTourismGrid />
  </GrhSection>

  {/* === 6) Veranstaltungen als Editorial-Liste ========================== */}
  <GrhSection tone="cream" padding="lg" eyebrow="Kalender" title="Demnächst in der Gemeinde." align="left">
    <GrhEventsList events={events} />
    <div slot="cta">
      <GrhButton href="/veranstaltungen" variant="ghost">Vollständiger Kalender</GrhButton>
    </div>
  </GrhSection>

  {/* === 7) News + Sperrungen ============================================ */}
  <GrhSection tone="paper" padding="lg" eyebrow="Aktuelles" title="Was uns gerade bewegt." align="left">
    <div class="grh-news2">
      {/* === Linke Hälfte: Editorial-News ============================ */}
      <div class="grh-news2__main">
        <a class="grh-news2__feature" href={featuredNews.href}>
          <div class="grh-news2__feature-media" data-cat={featuredNews.category}>
            {featuredNews.image ? (
              <img src={featuredNews.image} alt={featuredNews.title} loading="lazy" decoding="async" />
            ) : (
              <div class="grh-news2__feature-fallback" aria-hidden="true"></div>
            )}
            <span class="grh-news2__badge grh-news2__badge--lg">
              {categoryLabel[featuredNews.category]}
            </span>
          </div>
          <div class="grh-news2__feature-body">
            <time class="grh-news2__date">{fmtDate.format(featuredNews.date)}</time>
            <h3 class="grh-news2__feature-title">{featuredNews.title}</h3>
            <p class="grh-news2__feature-excerpt">{featuredNews.excerpt}</p>
            <span class="grh-news2__feature-cta">
              Weiterlesen <GrhIcon name="arrow-right" size={16} />
            </span>
          </div>
        </a>

        {otherNews.length > 0 && (
          <div class="grh-news2__grid">
            {otherNews.map((n) => (
              <a class="grh-news2__card" href={n.href}>
                <div class="grh-news2__card-media" data-cat={n.category}>
                  {n.image ? (
                    <img src={n.image} alt={n.title} loading="lazy" decoding="async" />
                  ) : (
                    <div class="grh-news2__card-fallback" aria-hidden="true"></div>
                  )}
                  <span class="grh-news2__badge">{categoryLabel[n.category]}</span>
                </div>
                <div class="grh-news2__card-body">
                  <time class="grh-news2__date">{fmtDate.format(n.date)}</time>
                  <h4 class="grh-news2__card-title">{n.title}</h4>
                </div>
              </a>
            ))}
          </div>
        )}
      </div>

      {/* === Rechte Hälfte: Sperrungen + Service ===================== */}
      <aside class="grh-news2__alerts">
        <header class="grh-news2__alerts-head">
          <span class="grh-eyebrow grh-news2__alerts-eyebrow">
            <GrhIcon name="map-pin" size={12} /> Verkehr &amp; Sperrungen
          </span>
          <h3>Was unterwegs zählt.</h3>
        </header>

        {sperrungen.length > 0 ? (
          <ul class="grh-news2__alert-list">
            {sperrungen.map((s) => (
              <li class={`grh-news2__alert grh-news2__alert--${s.severity ?? 'info'}`}>
                <a href={s.href} class="grh-news2__alert-link">
                  <span class="grh-news2__alert-dot" aria-hidden="true"></span>
                  <div class="grh-news2__alert-body">
                    <span class="grh-news2__alert-tag">
                      {severityLabel[s.severity ?? 'info']}
                      {s.ortsteil && s.ortsteil !== 'alle' && (
                        <> · {s.ortsteil.charAt(0).toUpperCase() + s.ortsteil.slice(1)}</>
                      )}
                    </span>
                    <h4 class="grh-news2__alert-title">{s.title}</h4>
                    {s.affectedStreet && (
                      <p class="grh-news2__alert-meta">
                        <GrhIcon name="map-pin" size={11} />
                        <span>{s.affectedStreet}</span>
                      </p>
                    )}
                    {s.detour && (
                      <p class="grh-news2__alert-meta">
                        <GrhIcon name="arrow-right" size={11} />
                        <span>{s.detour}</span>
                      </p>
                    )}
                    {s.validUntil && (
                      <p class="grh-news2__alert-until">
                        <GrhIcon name="clock" size={11} />
                        gültig bis {fmtUntil.format(s.validUntil)}
                      </p>
                    )}
                  </div>
                </a>
              </li>
            ))}
          </ul>
        ) : (
          <p class="grh-news2__alert-empty">Aktuell keine Sperrungen gemeldet.</p>
        )}

        <a href="/neuigkeiten/sperrungen" class="grh-news2__alerts-more">
          Alle Verkehrsmeldungen <GrhIcon name="arrow-right" size={12} />
        </a>

        <div class="grh-news2__service">
          <h4 class="grh-news2__service-title">Service &amp; Notfall</h4>
          <ul class="grh-news2__service-list">
            <li>
              <a href="/leben/maengelmelder">
                <span class="grh-news2__service-icon"><GrhIcon name="map-pin" size={14} /></span>
                <span class="grh-news2__service-label">
                  <strong>Schaden melden</strong>
                  <em>Schlagloch, Lampe, Müll</em>
                </span>
                <GrhIcon name="arrow-right" size={12} />
              </a>
            </li>
            <li>
              <a href="tel:+4937294170-0">
                <span class="grh-news2__service-icon"><GrhIcon name="phone" size={14} /></span>
                <span class="grh-news2__service-label">
                  <strong>Bauhof &amp; Bürgerbüro</strong>
                  <em>037294 / 170-0</em>
                </span>
                <GrhIcon name="arrow-right" size={12} />
              </a>
            </li>
            <li>
              <a href="tel:112">
                <span class="grh-news2__service-icon grh-news2__service-icon--alert"><GrhIcon name="phone" size={14} /></span>
                <span class="grh-news2__service-label">
                  <strong>Notruf 112</strong>
                  <em>Feuerwehr &amp; Rettungsdienst</em>
                </span>
                <GrhIcon name="arrow-right" size={12} />
              </a>
            </li>
          </ul>
        </div>
      </aside>
    </div>
  </GrhSection>

  {/* === 8) Vereine + Leben ============================================== */}
  <GrhSection tone="paper" padding="lg" eyebrow="Mittendrin" title="Vereinsleben und Alltag." subtitle="Was diese Gemeinde am Laufen hält — von der Feuerwehr bis zur Bücherei." align="left">
    <div class="grh-leben">
      {/* Stats-Reihe */}
      <ul class="grh-leben__stats">
        <li><strong>30+</strong><span>Vereine in allen drei Ortsteilen</span></li>
        <li><strong>3</strong><span>Kitas &amp; Hort-Einrichtungen</span></li>
        <li><strong>2</strong><span>Grundschulen vor Ort</span></li>
        <li><strong>4</strong><span>Freiwillige Feuerwehren</span></li>
      </ul>

      {/* Service-Grid */}
      <div class="grh-leben__grid">
        <a href="/leben/kita" class="grh-leben__tile">
          <span class="grh-leben__tile-icon"><GrhIcon name="building" size={22} /></span>
          <span class="grh-leben__tile-label">Kita &amp; Hort</span>
          <span class="grh-leben__tile-sub">Borstendorf · Grünhainichen</span>
        </a>
        <a href="/leben/grundschule" class="grh-leben__tile">
          <span class="grh-leben__tile-icon"><GrhIcon name="document" size={22} /></span>
          <span class="grh-leben__tile-label">Grundschule</span>
          <span class="grh-leben__tile-sub">Zwei Standorte</span>
        </a>
        <a href="/leben/kirche" class="grh-leben__tile">
          <span class="grh-leben__tile-icon"><GrhIcon name="building" size={22} /></span>
          <span class="grh-leben__tile-label">Kirche</span>
          <span class="grh-leben__tile-sub">Gemeinden &amp; Gottesdienste</span>
        </a>
        <a href="/leben/buecherei" class="grh-leben__tile">
          <span class="grh-leben__tile-icon"><GrhIcon name="document" size={22} /></span>
          <span class="grh-leben__tile-label">Bücherei</span>
          <span class="grh-leben__tile-sub">Lesen &amp; Ausleihen</span>
        </a>
        <a href="/leben/einkaufen" class="grh-leben__tile">
          <span class="grh-leben__tile-icon"><GrhIcon name="utensils" size={22} /></span>
          <span class="grh-leben__tile-label">Einkaufen</span>
          <span class="grh-leben__tile-sub">Hofläden &amp; Geschäfte</span>
        </a>
        <a href="/leben/heiraten" class="grh-leben__tile">
          <span class="grh-leben__tile-icon"><GrhIcon name="map-pin" size={22} /></span>
          <span class="grh-leben__tile-label">Heiraten</span>
          <span class="grh-leben__tile-sub">Standesamt vor Ort</span>
        </a>
      </div>

      <div class="grh-leben__ctas">
        <GrhButton href="/vereine" variant="primary" size="sm">Vereinsverzeichnis</GrhButton>
        <GrhButton href="/leben"   variant="ghost"   size="sm">Alle Service-Themen</GrhButton>
      </div>
    </div>
  </GrhSection>

  {/* === 10) Gewerbe-Statement =========================================== */}
  <GrhSection tone="forest" padding="lg" align="center" pattern="topo">
    <div class="grh-gewerbe">
      <span class="grh-eyebrow">Wirtschaftsstandort</span>
      <p class="grh-gewerbe__claim">
        <em>Eine prosperierende, breit aufgestellte Wirtschaft —</em>
        mit der erzgebirgischen Holzkunst als Wahrzeichen,
        modernen Handwerksbetrieben und einem starken Vereinsleben.
      </p>
      <div class="grh-gewerbe__cta">
        <GrhButton href="/gewerbe">Gewerbeverzeichnis</GrhButton>
        <GrhButton href="/gewerbe/stellenausschreibungen" variant="ghost">Stellenangebote</GrhButton>
      </div>
    </div>
  </GrhSection>

  {/* === Saisonal: Advent ================================================ */}
  {showAdventSection && (
    <GrhSection tone="forest-deep" padding="lg" eyebrow="Saisonal — Advent" title="Wenn die Fenster leuchten." align="left">
      <p class="grh-advent">
        Schwibbögen, Engel und Bergmann verwandeln Borstendorf, Grünhainichen und Waldkirchen
        in ein Lichtermeer. Erleben Sie unseren Lebendigen Adventskalender — jeden Abend öffnet
        sich in einem anderen Haus ein neues Türchen.
      </p>
      <div slot="cta">
        <GrhButton href="/leben/lebendiger-adventskalender">Programm Adventskalender</GrhButton>
      </div>
    </GrhSection>
  )}

  {/* === 11) Kontakt-/Rathaus-Block ====================================== */}
  <GrhSection tone="cream" padding="md" eyebrow="Rathaus" title="So erreichen Sie uns." align="left">
    <div class="grh-rathaus">
      <div class="grh-rathaus__col">
        <span class="grh-rathaus__label">Anschrift</span>
        <p>
          Gemeindeverwaltung Grünhainichen<br />
          Chemnitzer Straße 41<br />
          09579 Grünhainichen
        </p>
      </div>
      <div class="grh-rathaus__col">
        <span class="grh-rathaus__label">Kontakt</span>
        <p>
          <a href="tel:+4937294170-0">037294 / 170-0</a><br />
          <a href="mailto:gemeinde@gruenhainichen.com">gemeinde@gruenhainichen.com</a>
        </p>
      </div>
      <div class="grh-rathaus__col">
        <span class="grh-rathaus__label">Sprechzeiten</span>
        <p>
          Di 9–12 &amp; 13–18 · Do 9–12 &amp; 13–16 · Fr 9–12
        </p>
      </div>
      <div class="grh-rathaus__col">
        <span class="grh-rathaus__label">Bürgermeister</span>
        <p>nach Vereinbarung</p>
        <GrhButton href="/gemeinde/buergermeister" variant="ghost" size="sm">Termin vereinbaren</GrhButton>
      </div>
    </div>
  </GrhSection>

</BaseLayout>

<style>
  /* === Manifest ============================================== */
  .grh-manifest {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--grh-space-7);
  }
  @media (min-width: 980px) {
    .grh-manifest { grid-template-columns: 7fr 5fr; gap: var(--grh-space-9); }
  }
  .grh-manifest__head {
    font-family: var(--grh-font-display);
    font-size: clamp(2.4rem, 5vw, 4.4rem);
    line-height: 1.05;
    letter-spacing: -0.025em;
    color: var(--grh-forest-900);
    margin-top: var(--grh-space-3);
  }
  .grh-manifest__head em {
    display: block;
    font-style: italic;
    font-weight: 300;
    color: var(--grh-stone-700);
    font-size: 0.78em;
    margin-top: var(--grh-space-3);
  }
  .grh-manifest__copy {
    display: flex;
    flex-direction: column;
    gap: var(--grh-space-4);
    align-self: end;
  }
  .grh-manifest__copy p {
    font-size: var(--grh-fs-md);
    line-height: 1.7;
    color: var(--grh-stone-700);
  }
  .grh-manifest__sig {
    font-size: var(--grh-fs-sm) !important;
    letter-spacing: 0.04em;
    color: var(--grh-forest-800) !important;
  }
  .grh-manifest__sig strong { font-weight: 600; }

  /* === News v2 — Editorial-Spread + Alert-Spalte ============= */
  .grh-news2 {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--grh-space-7);
  }
  @media (min-width: 1024px) {
    .grh-news2 {
      grid-template-columns: minmax(0, 1.6fr) minmax(0, 1fr);
      gap: var(--grh-space-8);
    }
  }

  .grh-news2__main {
    display: flex;
    flex-direction: column;
    gap: var(--grh-space-6);
    min-width: 0;
  }

  /* Feature-Card — groß, Bild oben/links ====================== */
  .grh-news2__feature {
    display: grid;
    grid-template-columns: 1fr;
    text-decoration: none;
    color: inherit;
    background: var(--grh-cream-50);
    border: 1px solid var(--grh-line);
    border-radius: var(--grh-radius-lg);
    overflow: hidden;
    transition: transform var(--grh-dur-base), box-shadow var(--grh-dur-base), border-color var(--grh-dur-base);
  }
  @media (min-width: 720px) {
    .grh-news2__feature { grid-template-columns: 1.1fr 1fr; }
  }
  .grh-news2__feature:hover {
    transform: translateY(-3px);
    border-color: var(--grh-candle-500);
    box-shadow: var(--grh-shadow-md);
  }

  .grh-news2__feature-media {
    position: relative;
    aspect-ratio: 4 / 3;
    overflow: hidden;
    background: var(--grh-paper-100);
  }
  @media (min-width: 720px) {
    .grh-news2__feature-media { aspect-ratio: auto; min-height: 280px; }
  }
  .grh-news2__feature-media img,
  .grh-news2__card-media img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 1s var(--grh-ease-out);
  }
  .grh-news2__feature:hover .grh-news2__feature-media img,
  .grh-news2__card:hover .grh-news2__card-media img { transform: scale(1.04); }

  .grh-news2__feature-fallback,
  .grh-news2__card-fallback {
    position: absolute; inset: 0;
    background: linear-gradient(160deg, #2A6B43, #13301F);
  }
  /* Kategoriespezifische Fallback-Tints */
  [data-cat="tourismus"] .grh-news2__card-fallback,
  [data-cat="tourismus"] .grh-news2__feature-fallback {
    background: linear-gradient(160deg, #5BAE78, #2A6B43);
  }
  [data-cat="verwaltung"] .grh-news2__card-fallback,
  [data-cat="verwaltung"] .grh-news2__feature-fallback {
    background: linear-gradient(160deg, #4A6B7A, #2C4250);
  }
  [data-cat="veranstaltung"] .grh-news2__card-fallback,
  [data-cat="veranstaltung"] .grh-news2__feature-fallback {
    background: linear-gradient(160deg, #C9974A, #8C6224);
  }

  .grh-news2__badge {
    position: absolute;
    top: 12px;
    left: 12px;
    background: var(--grh-cream-50);
    color: var(--grh-forest-900);
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    font-weight: 700;
    padding: 5px 10px;
    border-radius: var(--grh-radius-pill);
    box-shadow: var(--grh-shadow-sm);
  }
  .grh-news2__badge--lg { font-size: 11px; padding: 6px 12px; }

  .grh-news2__feature-body {
    display: flex;
    flex-direction: column;
    gap: var(--grh-space-3);
    padding: var(--grh-space-6);
  }
  .grh-news2__date {
    font-family: var(--grh-font-display);
    font-style: italic;
    font-size: var(--grh-fs-sm);
    color: var(--grh-moss-600);
  }
  .grh-news2__feature-title {
    font-family: var(--grh-font-display);
    font-size: clamp(1.5rem, 2.6vw, 2.2rem);
    line-height: 1.15;
    color: var(--grh-forest-900);
    letter-spacing: -0.02em;
    margin: 0;
  }
  .grh-news2__feature-excerpt {
    font-size: var(--grh-fs-md);
    line-height: 1.65;
    color: var(--grh-stone-700);
    margin: 0;
    flex: 1;
  }
  .grh-news2__feature-cta {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-weight: 700;
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.22em;
    color: var(--grh-forest-800);
    border-bottom: 1.5px solid var(--grh-candle-500);
    padding-bottom: 4px;
    width: fit-content;
    margin-top: var(--grh-space-2);
    transition: gap var(--grh-dur-base), color var(--grh-dur-base);
  }
  .grh-news2__feature:hover .grh-news2__feature-cta {
    gap: 14px;
    color: var(--grh-moss-600);
  }

  /* 3-Card-Grid unter dem Feature ============================ */
  .grh-news2__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--grh-space-5);
  }
  @media (min-width: 540px)  { .grh-news2__grid { grid-template-columns: 1fr 1fr; } }
  @media (min-width: 1024px) { .grh-news2__grid { grid-template-columns: repeat(3, 1fr); } }

  .grh-news2__card {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    color: inherit;
    background: var(--grh-cream-50);
    border: 1px solid var(--grh-line);
    border-radius: var(--grh-radius-md);
    overflow: hidden;
    transition: transform var(--grh-dur-base), border-color var(--grh-dur-base), box-shadow var(--grh-dur-base);
  }
  .grh-news2__card:hover {
    transform: translateY(-3px);
    border-color: var(--grh-candle-500);
    box-shadow: var(--grh-shadow-sm);
  }
  .grh-news2__card-media {
    position: relative;
    aspect-ratio: 16 / 10;
    overflow: hidden;
    background: var(--grh-paper-100);
  }
  .grh-news2__card-body {
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: var(--grh-space-4);
  }
  .grh-news2__card-title {
    font-family: var(--grh-font-display);
    font-size: var(--grh-fs-md);
    line-height: 1.25;
    color: var(--grh-forest-900);
    letter-spacing: -0.01em;
    margin: 0;
    font-weight: 500;
  }

  /* === Alerts/Sperrungen-Spalte ============================== */
  .grh-news2__alerts {
    display: flex;
    flex-direction: column;
    gap: var(--grh-space-5);
    padding: var(--grh-space-6);
    background: var(--grh-cream-50);
    border: 1px solid var(--grh-line);
    border-top: 4px solid var(--grh-alert-600);
    border-radius: var(--grh-radius-md);
    align-self: start;
  }
  .grh-news2__alerts-head {
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding-bottom: var(--grh-space-3);
    border-bottom: 1px solid var(--grh-line);
  }
  .grh-news2__alerts-eyebrow {
    color: var(--grh-alert-600) !important;
    display: inline-flex !important;
    align-items: center;
    gap: 6px;
  }
  .grh-news2__alerts-head h3 {
    font-family: var(--grh-font-display);
    font-size: clamp(1.3rem, 2vw, 1.7rem);
    line-height: 1.2;
    color: var(--grh-forest-900);
    letter-spacing: -0.01em;
    margin: 0;
  }

  .grh-news2__alert-list {
    display: flex;
    flex-direction: column;
    gap: var(--grh-space-3);
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .grh-news2__alert {
    background: var(--grh-paper-100);
    border-radius: var(--grh-radius-sm, 6px);
    transition: background var(--grh-dur-fast);
  }
  .grh-news2__alert:hover { background: var(--grh-cream-50); }

  .grh-news2__alert-link {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--grh-space-3);
    align-items: start;
    padding: var(--grh-space-4);
    text-decoration: none;
    color: inherit;
  }
  .grh-news2__alert-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    margin-top: 6px;
    flex-shrink: 0;
    box-shadow: 0 0 0 4px rgba(255, 255, 255, 0.6);
  }
  .grh-news2__alert--info  .grh-news2__alert-dot { background: var(--grh-moss-600); }
  .grh-news2__alert--warn  .grh-news2__alert-dot { background: #C9974A; }
  .grh-news2__alert--alert .grh-news2__alert-dot {
    background: var(--grh-alert-600);
    animation: grh-news2-pulse 2.4s ease-in-out infinite;
  }
  @keyframes grh-news2-pulse {
    0%, 100% { box-shadow: 0 0 0 4px rgba(255, 255, 255, 0.6); }
    50%      { box-shadow: 0 0 0 4px rgba(176, 47, 47, 0.25); }
  }

  .grh-news2__alert-body {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .grh-news2__alert-tag {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--grh-stone-400);
    font-weight: 700;
  }
  .grh-news2__alert--alert .grh-news2__alert-tag { color: var(--grh-alert-600); }
  .grh-news2__alert-title {
    font-family: var(--grh-font-display);
    font-size: var(--grh-fs-md);
    line-height: 1.25;
    color: var(--grh-forest-900);
    letter-spacing: -0.01em;
    margin: 0;
    font-weight: 500;
  }
  .grh-news2__alert-meta,
  .grh-news2__alert-until {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: var(--grh-fs-sm);
    line-height: 1.45;
    color: var(--grh-stone-700);
    margin: 0;
  }
  .grh-news2__alert-until {
    margin-top: 4px;
    color: var(--grh-moss-600);
    font-weight: 600;
    font-size: 12px;
  }

  .grh-news2__alert-empty {
    font-size: var(--grh-fs-sm);
    color: var(--grh-stone-700);
    font-style: italic;
    line-height: 1.5;
  }

  .grh-news2__alerts-more {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    color: var(--grh-alert-600);
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.22em;
    font-weight: 700;
    text-decoration: none;
    align-self: flex-start;
    transition: gap var(--grh-dur-base);
  }
  .grh-news2__alerts-more:hover { gap: 12px; }

  /* Service & Notfall-Box innerhalb der Alert-Spalte ========== */
  .grh-news2__service {
    border-top: 1px solid var(--grh-line);
    padding-top: var(--grh-space-4);
    margin-top: var(--grh-space-2);
  }
  .grh-news2__service-title {
    font-family: var(--grh-font-body);
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.22em;
    color: var(--grh-moss-600);
    font-weight: 700;
    margin: 0 0 var(--grh-space-3);
  }
  .grh-news2__service-list {
    display: flex;
    flex-direction: column;
    gap: 6px;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .grh-news2__service-list a {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: var(--grh-space-3);
    align-items: center;
    padding: 10px 12px;
    text-decoration: none;
    color: inherit;
    border-radius: var(--grh-radius-sm, 6px);
    transition: background var(--grh-dur-fast);
  }
  .grh-news2__service-list a:hover {
    background: var(--grh-paper-100);
  }
  .grh-news2__service-icon {
    width: 32px;
    height: 32px;
    display: grid;
    place-items: center;
    border-radius: 50%;
    background: var(--grh-paper-100);
    color: var(--grh-forest-800);
    flex-shrink: 0;
  }
  .grh-news2__service-icon--alert {
    background: rgba(176, 47, 47, 0.12);
    color: var(--grh-alert-600);
  }
  .grh-news2__service-label {
    display: flex;
    flex-direction: column;
    gap: 2px;
    line-height: 1.3;
    min-width: 0;
  }
  .grh-news2__service-label strong {
    font-size: var(--grh-fs-sm);
    color: var(--grh-forest-900);
    font-weight: 600;
  }
  .grh-news2__service-label em {
    font-style: normal;
    font-size: 11px;
    color: var(--grh-stone-400);
    letter-spacing: 0.04em;
  }

  /* === Vereine & Leben ======================================= */
  .grh-leben {
    display: flex;
    flex-direction: column;
    gap: var(--grh-space-7);
  }

  /* Stats-Reihe oben */
  .grh-leben__stats {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0;
    list-style: none;
    margin: 0;
    padding: 0;
    border-top: 1px solid var(--grh-line);
    border-bottom: 1px solid var(--grh-line);
  }
  @media (min-width: 720px) { .grh-leben__stats { grid-template-columns: repeat(4, 1fr); } }
  .grh-leben__stats li {
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: var(--grh-space-5) var(--grh-space-4);
    border-right: 1px solid var(--grh-line);
  }
  .grh-leben__stats li:nth-child(2n) { border-right: none; }
  @media (min-width: 720px) {
    .grh-leben__stats li { border-right: 1px solid var(--grh-line); }
    .grh-leben__stats li:nth-child(2n) { border-right: 1px solid var(--grh-line); }
    .grh-leben__stats li:last-child { border-right: none; }
  }
  .grh-leben__stats strong {
    font-family: var(--grh-font-display);
    font-style: italic;
    font-weight: 400;
    font-size: clamp(2.4rem, 5vw, 3.4rem);
    line-height: 0.9;
    color: var(--grh-forest-800);
    letter-spacing: -0.02em;
  }
  .grh-leben__stats span {
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: var(--grh-stone-400);
    line-height: 1.4;
  }

  /* Service-Grid */
  .grh-leben__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--grh-space-3);
  }
  @media (min-width: 540px)  { .grh-leben__grid { grid-template-columns: 1fr 1fr; } }
  @media (min-width: 900px)  { .grh-leben__grid { grid-template-columns: repeat(3, 1fr); } }
  @media (min-width: 1200px) { .grh-leben__grid { grid-template-columns: repeat(6, 1fr); } }

  .grh-leben__tile {
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding: var(--grh-space-5) var(--grh-space-4);
    background: var(--grh-cream-50);
    border: 1px solid var(--grh-line);
    border-radius: var(--grh-radius-md);
    text-decoration: none;
    color: inherit;
    transition: transform var(--grh-dur-fast), border-color var(--grh-dur-fast), background var(--grh-dur-fast);
  }
  .grh-leben__tile:hover {
    transform: translateY(-3px);
    border-color: var(--grh-moss-500);
    background: var(--grh-sage-100);
  }
  .grh-leben__tile-icon {
    width: 44px; height: 44px;
    border-radius: 50%;
    background: var(--grh-paper-100);
    color: var(--grh-forest-800);
    display: grid; place-items: center;
    margin-bottom: var(--grh-space-2);
    transition: background var(--grh-dur-fast), color var(--grh-dur-fast);
  }
  .grh-leben__tile:hover .grh-leben__tile-icon {
    background: var(--grh-forest-800);
    color: var(--grh-candle-500);
  }
  .grh-leben__tile-label {
    font-family: var(--grh-font-display);
    font-size: var(--grh-fs-md);
    font-weight: 500;
    color: var(--grh-forest-900);
    line-height: 1.2;
    letter-spacing: -0.01em;
  }
  .grh-leben__tile-sub {
    font-size: 11px;
    color: var(--grh-stone-400);
    text-transform: uppercase;
    letter-spacing: 0.16em;
  }

  .grh-leben__ctas {
    display: flex;
    flex-wrap: wrap;
    gap: var(--grh-space-3);
    margin-top: var(--grh-space-2);
  }

  /* === Gewerbe Statement ===================================== */
  .grh-gewerbe {
    text-align: center;
    max-width: 720px;
    margin-inline: auto;
  }
  .grh-gewerbe__claim {
    font-family: var(--grh-font-display);
    font-size: clamp(1.8rem, 3.5vw, 2.6rem);
    line-height: 1.25;
    color: var(--grh-cream-50);
    margin: var(--grh-space-5) 0 var(--grh-space-7);
    letter-spacing: -0.015em;
  }
  .grh-gewerbe__claim em {
    color: var(--grh-candle-500);
    font-style: italic;
    font-weight: 400;
    display: block;
    margin-bottom: 0.4em;
  }
  .grh-gewerbe__cta {
    display: flex; flex-wrap: wrap; gap: var(--grh-space-3); justify-content: center;
  }
  /* Buttons auf dunklem Forest-Hintergrund: helle Varianten erzwingen */
  .grh-gewerbe__cta :global(.grh-btn--primary) {
    --_bg: var(--grh-candle-500);
    --_fg: var(--grh-forest-900);
  }
  .grh-gewerbe__cta :global(.grh-btn--primary:hover) {
    --_bg: var(--grh-cream-50);
  }
  .grh-gewerbe__cta :global(.grh-btn--ghost) {
    --_bg: transparent;
    --_fg: var(--grh-cream-50);
    --_border: rgba(250, 247, 241, 0.45);
  }
  .grh-gewerbe__cta :global(.grh-btn--ghost:hover) {
    --_bg: rgba(250, 247, 241, 0.08);
    --_border: var(--grh-candle-500);
    --_fg: var(--grh-candle-500);
  }

  /* === Advent ================================================ */
  .grh-advent {
    font-family: var(--grh-font-display);
    font-style: italic;
    font-size: clamp(1.6rem, 2.8vw, 2.2rem);
    line-height: 1.4;
    max-width: 60ch;
    color: var(--grh-cream-50);
    opacity: 0.92;
  }

  /* === Rathaus =============================================== */
  .grh-rathaus {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(220px, 100%), 1fr));
    gap: var(--grh-space-6);
    border-top: 1px solid var(--grh-line);
    padding-top: var(--grh-space-6);
  }
  .grh-rathaus__col { display: flex; flex-direction: column; gap: var(--grh-space-2); }
  .grh-rathaus__label {
    font-size: 11px; text-transform: uppercase; letter-spacing: 0.22em;
    color: var(--grh-moss-600); font-weight: 600;
  }
  .grh-rathaus__col p { font-size: var(--grh-fs-sm); line-height: 1.7; color: var(--grh-stone-700); }
  .grh-rathaus__col a { color: var(--grh-forest-800); border-bottom: 1px solid transparent; }
  .grh-rathaus__col a:hover { border-color: var(--grh-candle-500); }

  /* === Stacking Cards (Sektionen 02 / 03 / 04) =============== */
  .grh-cardstack {
    display: flex;
    flex-direction: column;
    gap: 0;
  }
  .grh-cardstack__card {
    position: sticky;
    border-radius: var(--grh-radius-xl);
    overflow: hidden;
    isolation: isolate;
    box-shadow:
      0 -10px 40px -22px rgba(14, 31, 24, 0.45),
      0 22px 50px -28px rgba(14, 31, 24, 0.35);
  }
  /* Progressiver Fächer mit 4 Karten — jede breiter als die vorige.
     Karte 1 = schmalste (unterste), Karte 4 = breiteste (oberste). */
  .grh-cardstack__card--1 {
    top: calc(var(--grh-space-7));
    z-index: 1;
    margin-inline: var(--grh-space-9);
  }
  .grh-cardstack__card--2 {
    top: calc(var(--grh-space-7) + 36px);
    z-index: 2;
    margin-inline: var(--grh-space-7);
  }
  .grh-cardstack__card--3 {
    top: calc(var(--grh-space-7) + 72px);
    z-index: 3;
    margin-inline: var(--grh-space-5);
  }
  .grh-cardstack__card--4 {
    top: calc(var(--grh-space-7) + 108px);
    z-index: 4;
    margin-inline: var(--grh-space-2);
  }
  @media (min-width: 1100px) {
    .grh-cardstack__card--1 { margin-inline: var(--grh-space-10); }
    .grh-cardstack__card--2 { margin-inline: var(--grh-space-8); }
    .grh-cardstack__card--3 { margin-inline: var(--grh-space-6); }
    .grh-cardstack__card--4 { margin-inline: var(--grh-space-3); }
  }

  /* Abstand zwischen den Karten erzeugt den "Reise"-Scrollweg —
     je größer, desto langsamer rutschen sie übereinander */
  .grh-cardstack__card + .grh-cardstack__card { margin-top: var(--grh-space-9); }

  /* Erzeugte Karten beziehen ihren Hintergrund jetzt aus der inneren
     Section — nichts bleibt full-bleed, alles innerhalb der gerundeten Karte */
  .grh-cardstack__card :global(.grh-section) { border-radius: inherit; }

  /* Innen-Padding der Sections in der Karte deutlich reduzieren, damit
     der Inhalt jeder Karte in eine Viewport-Höhe passt und nicht von der
     nächsten Karte verdeckt wird, bevor man den Boden lesen kann. */
  .grh-cardstack__card :global(.grh-section--pad-lg) {
    padding-block: var(--grh-space-6);
  }
  @media (min-width: 1100px) {
    .grh-cardstack__card :global(.grh-section--pad-lg) {
      padding-block: var(--grh-space-7);
    }
  }
  /* TraditionShowcase: vertikales Padding der inneren spread2-Article kürzen */
  .grh-cardstack__card :global(.grh-spread2) {
    padding-block: var(--grh-space-4);
  }

  /* Mobile: Stacking deaktivieren, normale Sektionen */
  @media (max-width: 900px) {
    .grh-cardstack__card {
      position: relative;
      top: auto;
      margin-inline: 0;
      border-radius: 0;
      box-shadow: none;
    }
    .grh-cardstack__card + .grh-cardstack__card { margin-top: 0; }
  }

</style>
